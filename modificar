<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8">
  <title>Forja de Heróis</title>
  <style>
    body {
      background: #201a14 url('https://www.transparenttextures.com/patterns/wood-pattern.png');
      color: #f4e9d8;
      font-family: "Cinzel", serif;
      text-align: center;
      padding: 2rem;
    }

    h1 {
      font-size: 2rem;
      margin-bottom: 1rem;
      color: #e2b714;
      text-shadow: 1px 1px 4px #000;
    }

    .painel {
      background: rgba(0,0,0,0.5);
      border: 2px solid #a97c50;
      border-radius: 8px;
      display: inline-block;
      padding: 1rem;
      margin-top: 1rem;
      box-shadow: 0 0 10px #000;
    }

    label, select {
      font-size: 1rem;
      margin: 0.5rem;
      color: #fff0d9;
    }

    select {
      background: #3e2f24;
      border: 1px solid #c0a97d;
      padding: 0.3rem 0.6rem;
      border-radius: 4px;
    }

    img {
      margin-top: 1.5rem;
      image-rendering: pixelated;
      border: 5px solid #2a1d14;
      background: #000;
    }
  </style>

  <!-- Fonte medieval -->
  <link href="https://fonts.googleapis.com/css2?family=Cinzel:wght@600&display=swap" rel="stylesheet">
</head>
<body>
  <h1>🛡️ Forja de Heróis</h1>

  <div class="painel">
    <label for="genero">Gênero:</label>
    <select id="genero">
      <option value="male">Masculino</option>
      <option value="female">Feminino</option>
    </select>

    <label for="raca">Raça:</label>
    <select id="raca">
      <option value="white">Humano</option>
      <option value="black">Humano Negro</option>
      <option value="elf">Elfo</option>
      <option value="lizard">Lagarto</option>
    </select>

    <label for="classe">Classe:</label>
    <select id="classe">
      <option value="fighter">Guerreiro</option>
      <option value="mage">Mago</option>
      <option value="cleric">Clérigo</option>
    </select>

    <br>

    <img id="personagem" src="" width="64" height="64" alt="Visual do Personagem">
  </div>

  <script>
    const imagem = document.getElementById("personagem");
    const genero = document.getElementById("genero");
    const raca = document.getElementById("raca");
    const classe = document.getElementById("classe");

    function atualizarSprite() {
      const urlBase = "https://raw.githubusercontent.com/sanderfrenken/Universal-LPC-Spritesheet-Character-Generator/master/spritesheets/";
      const nomeArquivo = `${genero.value}_${raca.value}_${classe.value}.png`;
      imagem.src = urlBase + nomeArquivo;
    }

    // Atualiza a imagem ao mudar qualquer seleção
    genero.onchange = raca.onchange = classe.onchange = atualizarSprite;

    // Inicializa
    atualizarSprite();
  </script>
</body>
</html>
